POSTGRES_USER=riskapi
POSTGRES_PASSWORD=          # set a strong password
POSTGRES_DB=riskdb
POSTGRES_HOST=postgres      # must match the Compose service name
POSTGRES_PORT=5432
API_KEY=                    # any non-empty string; used in X-API-Key header
